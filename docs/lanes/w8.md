# Lane handoff — W8, the two defects a real-browser pass found on the Throwaway store

Branch `day2/w8-neutral-brand` off `hub/main` `5c1b6a4`. Worktree `/Users/ludo/wt-w8-dash`.
Proof pack (verbatim red/green output): `~/tasks/multistore-hub/briefs/out/PROOF-W8.md`.
Screenshots: `briefs/out/w8-shots/`. Raw logs: `briefs/out/w8-logs/`.
Databases: `dash_w8_test`, `w8_sso`, `lane_w8_sso` on `127.0.0.1:5433`, all mine, all dropped at the end (R43).
Nothing pushed, nothing deployed, no live service and no live database touched.

Both defects are the same class: R15/R5, **store identity is data, not a build.**

---

# W8a — a brand-new store wore another store's wordmark

## The defect, measured

`scripts/browser-check-sidebar-switcher.mjs`, fixture 8a, on the tree before this lane — a runtime brand
answering `logoWhite: null` (which is exactly what the provisioner creates: it sets `BRAND_NAME`,
`BRAND_SHORT_NAME`, `BRAND_EMAIL_DOMAIN` and no logo urls):

```
8a. no logo, switcher trigger: {"surface":"store-switcher-button","imgs":["/logo-white.png"], "wordmark":null, ...}
8b. no logo, collapsed:        {"imgs":["/logo-symbol-white.png"], ...}
8c. no logo, no hub:           {"surface":"brand-block","imgs":["/logo-white.png"], ...}
```

`/logo-white.png` and `/logo-symbol-white.png` are one particular store's own bundled images in
`client/public`. The chain: `GET /api/v1/brand` answers null per unset key (correct — the server invents no
brand default, that would itself be a store literal in engine code); `client/src/config/brand.js` `apply()`
falls back per field to `FALLBACK`; and `FALLBACK`'s defaults were `'Mineblock LLC'` / `'Mineblock'` /
`'/logo-white.png'` / `'/logo-symbol-white.png'` / `'/logo-black.svg'` / `'mineblock.com'`.

## What changed

1. **`client/src/config/brand.js`** — the build-time `FALLBACK` carries no store: `name`/`shortName`
   default to the neutral placeholder `'Store'` (exported as `NEUTRAL_NAME`), every logo defaults to
   **`null`**, `emailDomain` to the RFC 2606 reserved `example.com`. **Every `VITE_BRAND_*` read is
   untouched** — only the right-hand side of each `||` moved. `apply()` is now exported as `applyBrand()`
   so a test can drive the runtime path without an HTTP server; `load()` is still its only production caller
   and `GET /api/v1/brand`'s contract is unchanged (same six keys, still all null with no env).

2. **`client/src/hooks/useBrand.js`** (new) — the runtime brand as React state. `brand.js` exports LIVE
   BINDINGS, and a component that reads a binding directly shows whatever it held at its last render. That
   was invisible while the build-time default was a real image and stops being invisible the moment the
   default is "no image at all", so the components that paint the brand now subscribe.

3. **`client/src/components/layout/StoreSwitcher.jsx`** — `BrandMark` no longer assumes a store has an
   image. With a logo it renders the image exactly as before. With **no** logo it renders a neutral text
   wordmark: the small rounded initial square plus the store's own short name in the sidebar's own type,
   and **no `<img>` at all** (a null `src` is a request for the current page and paints a broken-image
   glyph). Collapsed, where there is no room for a name, the initial square alone. This is the one place the
   wordmark lives, so the switcher trigger and the R21 plain brand block answer identically (asserted).

4. **`client/src/pages/Login.jsx`** — the login page did `<img src={BRAND_LOGO_WHITE}>`. Same rule: the
   image when there is one, the store's name as a heading when there is not. The email placeholder now uses
   `brand.emailDomain` instead of guessing `you@<shortname>.co`.

5. **`client/src/pages/public/SupplierPublicSheet.jsx`** — same for `BRAND_LOGO_BLACK` in the printed
   header, plus the two `BRAND_SHORT_NAME` strings, all through `useBrand()`.

6. **`client/src/components/settings/WorkspaceTab.jsx`** — the Workspace tab's name field was the literal
   `'Mineblock LLC'`; it now shows the runtime brand's name. Its placeholder member list (this tab is not
   wired to the team API) and `ProfileTab` / `ops/APIRuns` example addresses moved from `@mineblock.io` to
   `@example.com`.

## Proof

- `server/tests/store-config/neutral-brand.mjs` (new, 8 tests): the neutral FALLBACK, the runtime path
  per field, **the VITE_BRAND_\* reads still present**, the R15 grep over `brand.js` + the two sidebar
  files **with a positive control that bites on the exact lines this lane removed**, `git grep` as the
  reviewer would run it, and `/api/v1/brand`'s unchanged contract. RED 4/8 → GREEN 8/8.
- `scripts/browser-check-sidebar-switcher.mjs`: 37 → **46 checks**. Two brand fixtures (`logo`, `nologo`),
  four passes: the trigger, the collapsed trigger, the R21 plain block, and a **positive control** that a
  store WITH a logo still draws the image from the url the server sent. RED 8 of the 9 new checks fail
  (with `/logo-white.png` printed); GREEN 46/46.
- The VITE path proved by execution, not by reading: `VITE_BRAND_*=…` `npm run build` puts
  `VITE_BRAND_LOGO_WHITE:\`/logo-white.png\`` back into the bundle. Verbatim in PROOF-W8.md.

## `server/tests/store-config/wiring.mjs` — the pinned residue

Checked: `wiring.mjs`'s A1 grep runs over **`server/src` only**, and its pattern (`17cca0-2|…|mineblock\.co|…`)
does not carry `Mineblock` or the two logo paths. The literals this lane removed were never pinned there, so
**there was nothing to update** and `A1_KNOWN_OUT_OF_LANE` is unchanged (its two entries are both still
present, which that test also asserts). The new R15 grep for the client's brand files lives in
`neutral-brand.mjs` instead.

---

# W8b — the hub's JIT user met a red 403 on the store's own home page

## The defect, measured

Two independent causes, both proved red before the fix.

**Client:** `client/src/pages/Dashboard.jsx` called `api.get('/kpi-system/home-dashboard')` regardless of who
was looking, and `server/src/routes/kpiSystem.js:259` gates that whole router on
`requirePermission('kpi-system', 'access')`. `scripts/browser-check-home-permissions.mjs`, fixture B, before
the fix: `requests=1 gateCard=false red={"banner":true,…}`.

**Server:** migration 126 seeded `hub_role_map` against `seeds/seed_roles.js`
(`owner → 'Admin'`, `operator/editor → 'Manager'`). Those are the platform's ACCOUNT-administration roles;
the dashboard's pages are gated on the PAGE keys migration 031 seeds (`dashboard`, `brief-pipeline`, …) and
**neither `Admin` nor `Manager` carries a single page key**. So the operator the hub calls the OWNER of a
store arrived with a role that opens nothing in the sidebar. `server/tests/hub-sso/w8-jit-role.mjs` before
the fix: `J1: hub owner -> Team - Full Access … got ["Admin"]`.

## What changed

1. **`client/src/pages/Dashboard.jsx`** — the page takes `usePermissions()` and does not make the request it
   is not allowed to make. `gated` is a STATE, not an error: it is set from the user's own permissions
   before any request, **and** from a 403 the server answers (the server is the authority; the client's copy
   of a permission can be stale). While gated the KPI block, the Amazon row, the profitability panel, the
   break-even chart, the daily breakdown and the revenue chart are replaced by one quiet `KpiNoAccess` card,
   the Live pill and the date picker are hidden (they control nothing), and the 60 s poll does not run.
   **The retry banner is untouched and is still shown for a real failure** — fixture C proves it.
   `/kpi-system/home-dashboard` is the only permission-bound request this page makes (grepped).

2. **`server/migrations/133_hub_role_map_page_roles.sql`** (new, additive, idempotent, runs on an empty
   database) — re-points the map. Each `UPDATE` matches **only** the row that still holds migration 126's
   exact seeded value (so a store that already re-pointed a row by hand is left alone — migration 131's
   guard shape) and requires the target role NAME to exist (a dangling name is a 403 on every hop). A second
   run matches nothing. `order.json` += one entry at the end (117 entries).

3. **`server/src/routes/hubSso.js`** — the map lookup is now one function, `mappedRoleName()`, taken by both
   the JIT creation and the new divergence audit. Behaviour of the lookup is unchanged (exact hub role, else
   the `'*'` row, else refuse). New: when an EXISTING user's roles do not contain the role the map would have
   created, one `HUB_SSO_ROLE_UNCHANGED` audit row records `{mapped_role, held_roles}`. Nothing is changed by
   it. A user whose roles already match writes no row (negative control asserted) — a log that always fires
   says nothing.

## THE MAPPING, AND WHY

| hub role | dashboard role | why |
|---|---|---|
| `owner` | **Team - Full Access** | The store's full-access role as named in the roles table (migration 031, "Access to all dashboard pages", + `orders:access` from 086). It is the only seeded role that actually opens the dashboard's pages. |
| `admin` | **Team - Full Access** | A hub admin administers the STORE. The dashboard's own `Admin` role is the USER TABLE's administrator (users / departments / audit / settings) and opens no page — a different job, and the more dangerous one to hand out by SSO. |
| `operator` | **Team - Production** | The next one down in the existing set: dashboard + brief pipeline + brief agent + magic ads + statics. No revenue pages (asserted: no `orders`, no `kpi-system`). |
| `editor` | **Team - Production** | Same work, same tools. |
| `viewer` | **Viewer** | Read-only: asserted to carry no write action on any key. |
| `*` (anything else) | **Viewer** | **Fail closed.** An unknown hub role, and a ticket with no role at all, both land here (asserted). A hub role that spells a dashboard role name selects nothing — the map is the only door (asserted). |

**Idempotence / no silent downgrade.** An existing user is never re-roled, in either direction: the store's
own decision about an account outranks the hub's. A second hop changes nothing and creates nothing
(`HUB_SSO_JIT_CREATE` fires exactly once, asserted). A user whose roles were deliberately changed by the
store keeps them — and the divergence is now written to `audit_logs` so "why does my owner see nothing" has a
trail. **A user with ZERO roles is deliberately NOT topped up**: stripping roles is a plausible way a store
soft-disables somebody, and granting the mapped role there would be fail-OPEN.

**DECISION MADE — `kpi-system:access` is NOT granted to `Team - Full Access`, and no role's permissions are
touched by this lane.** `kpi-system` (the "CEO Office" / the home page's KPI block) is granted to **no role
but `SuperAdmin`** anywhere in this repo's migrations — checked: `grep -rn "kpi-system" server/migrations`
is empty. That reads as a deliberate restriction, not an oversight, so widening it would be a silent
privilege grant to every existing holder of that role on the two live dashboards. The consequence, stated
plainly: **a hub owner landing on a new store sees the home page with its KPI block replaced by the quiet
"not available for your role" card.** That is now a designed state instead of a red error. If the lead wants
owners to see the figures, it is one line, per store, on that store's database:

```sql
UPDATE roles SET permissions = permissions || '{"kpi-system":["access"]}'::jsonb
 WHERE name = 'Team - Full Access';
```

`docs/HUB-SSO.md` **does not exist on this tree** (checked), so the mapping is documented here and, in the
form that ships with the code, in the header of `server/migrations/133_hub_role_map_page_roles.sql`. Lane E's
`docs/lanes/lane-e.md` still describes migration 126's seed and was not edited (another lane's handoff file).

## Proof

- `server/tests/hub-sso/w8-jit-role.mjs` (new, 20 assertions, real router, real migrations off disk, fresh
  Postgres): RED 15/6 → GREEN 20/0.
- `scripts/browser-check-home-permissions.mjs` (new, 18 checks, real headless Chromium, no network): four
  fixtures — with the permission, without it, network down, and a 403 the client did not predict. The "no red
  error" assertion is a SHAPE scan (any axios failure text anywhere in `document.body.innerText`, plus any
  element computed in the shell's error red), not a test id, so the fix cannot be "rename the banner".
  RED 8/18 fail → GREEN 18/18.
- `client/dev/switcher-check.jsx` gained `?view=dashboard` and `?perms=none`, so the harness mounts the REAL
  Dashboard in the REAL shell with the REAL `usePermissions` hook. The `perms=none` identity is exactly what
  migration 133 maps a hub owner onto.

---

## REPORTED, NOT FIXED — other places a new store still meets another store's name

All in files outside this lane's two concerns (R25). None of them is the brand fallback; each is its own
literal, and each needs the store-data treatment in whichever lane owns the file.

| where | what |
|---|---|
| `client/src/pages/production/ClickupPipeline.jsx:4` | `const PIPELINE_URL = 'https://mineblock-video-launcher.onrender.com'` — a FUNCTIONAL per-store url hardcoded in engine code. The most serious of these: on any other store this page points at another store's tool. |
| `client/src/pages/production/BriefPipeline.jsx:665` | user-facing error copy: "re-import a real Mineblock ad". |
| `client/src/pages/production/briefs/MetaVideoImportModal.jsx:539` | user-facing copy: "syncing only image creatives into Mineblock". |
| `client/src/pages/production/briefs/LaunchTemplateEditor.jsx:1155` | input `placeholder="https://mineblock.co/pages/list-v1"`. |
| `client/src/pages/production/statics/defaultProduct.js:28,30,55` | comments naming the store (R15 counts comments — W6c P2-7). |
| `client/src/pages/production/briefs/LaunchTemplatesListModal.jsx:3`, `client/src/index.css:46` | comments naming the store / its tool. |
| `client/index.html:7` | `<title>client</title>` — not a store name, but not the store's name either: every store's browser tab says "client". `applyBrand()` already sets `<html data-brand>` from the runtime brand and could set `document.title` the same way; that is a one-line slice nobody has taken. |
| `server/src` (28 hits) | `slackAlert.js:60`, `metaWebhook.js:94`, `kpiSystem.js:118,2581,3004`, `briefAgent.js:1126`, `adRejectionMonitor.js:161` all post to Slack as `username: 'Mineblock Bot'` / `'Mineblock Watchdog'`; `clickupWebhook.js:2205,2460` default `FRAMEIO_LEGIT_PROJECT_NAMES` to `'Mineblock LLC'`; `salesTools.js:202` serves `<title>Mineblock — Payment Link Generator</title>` and `:7` pins a Whop product id. The Slack usernames are the cheapest real win: one `storeConfig` getter. |
| **email templates** | **none exist.** Checked: this repository sends no email — no `nodemailer`, no `sendEmail`, no transporter, and `BRAND_EMAIL_DOMAIN` is read in exactly one place (`server/src/config/storeConfig.js:102`). Nothing to fix and nothing to report beyond that. |

## Also worth the lead's attention

- **The A6 scope guard skipped**, as W6c's lead decision says it does off the store-code lane: the branch is
  `day2/w8-neutral-brand`, which does not match `/store-code/`, so
  `server/tests/store-code/a6-no-read-path.test.mjs` printed its SKIP line and passed. Its second test (the
  migration runner's contract) ran. Set `A6_ENFORCE=1` on the integration branch.
- **The 375 px shell overflow is still open** (`scrollWidth 571` vs `clientWidth 375`, measured again in this
  lane's browser run, identical with the switcher present and absent). It pre-dates W6, W6c left it open, and
  this lane did not touch it. Ludo's standing mobile rule is still not met by the dashboard shell.
- `client/src/components/auth/LoginPage.jsx` is **dead code** — nothing imports it — and still reads
  `BRAND_NAME` / `BRAND_EMAIL_DOMAIN` directly. It renders text only, so it now says "Store" rather than
  another store's name; it was left alone rather than half-fixed. Deleting it is its own decision.

---

# W8c — Ludo's decision: the hub OWNER sees the KPIs of the store it enters

Same branch `day2/w8-neutral-brand`, same worktree. Proof pack: `~/tasks/multistore-hub/briefs/out/PROOF-W8.md`
sections **15–21**. Database `dash_w8c_test` + two throwaway clones of `mineblock_copy`, all dropped (R43).
Nothing pushed, nothing deployed, no live service and no live database touched. **No client file changed.**

## The ask, and the constraint that shaped the answer

**Ludo, 2026-09-11:** the hub OWNER must see the KPIs of the store they enter — per store; there is no
cross-store view. Those KPIs are the CEO Office page **and** the home page's KPI cards (total sales, ad
spend, ROAS, purchases, AOV, profit), and both sit behind ONE permission key: `kpi-system:access`
(`server/src/routes/kpiSystem.js:259` gates the whole router; `client/src/App.jsx:212-214` and
`Sidebar.jsx:64,97,98` gate the pages on the same key).

**W8b's review concern, which still stands:** do not widen an existing role. `Team - Full Access` is held
by real people on the two live dashboards, so the one-line `UPDATE roles SET permissions = permissions ||
'{"kpi-system":["access"]}'` that W8b's handoff offers as the store-side remedy is a silent privilege grant
to every one of them.

**So W8c widens nothing. It mints a role.**

```
Hub Owner  =  Team - Full Access's permission set, COPIED AT MIGRATION TIME,  +  kpi-system:access
```

and re-points exactly one row of `hub_role_map`: `owner -> Hub Owner`. Nobody holds `Hub Owner` until the
hub JIT-creates a user through it, so on an existing database the migration changes no user's roles and no
existing role's permissions. Measured on a clone of the live Mineblock database, the whole footprint of
`server/migrations/134_hub_owner_kpi_role.sql` is: **one new role held by zero users, and one map row
re-pointed** (PROOF-W8 §18).

## THE MAPPING AFTER 134

| hub role | dashboard role | changed by 134? | why |
|---|---|---|---|
| `owner` | **Hub Owner** | **YES** | Ludo's decision. Everything `Team - Full Access` opens, plus the store's own KPIs. |
| `admin` | Team - Full Access | no | see the decision below |
| `operator` | Team - Production | no | 133's mapping, untouched |
| `editor` | Team - Production | no | 133's mapping, untouched |
| `viewer` | Viewer | no | read-only |
| `*` | Viewer | no | fail closed |

Asserted as a whole-map equality (J9), not row by row, so a future migration that moves a second row fails
this test rather than passing it quietly.

## DECISIONS MADE

**1. `admin` is NOT moved to `Hub Owner`.** Ludo's decision names the OWNER. This lane doc (W8b, §"THE
MAPPING, AND WHY") argues that a hub admin "administers the STORE, not the user table" — which is why 133
gave it the full-access role — but it does not argue that a hub admin is the store's financial principal,
and the revenue figures are the one thing this codebase has always kept behind their own key (before 134,
`kpi-system` was granted to no role but SuperAdmin anywhere in the migrations). Conservative reading; one
row reverses it, no deploy:

```sql
UPDATE hub_role_map SET dashboard_role = 'Hub Owner', updated_at = NOW() WHERE hub_role = 'admin';
```

**2. A COPY, not an inheritance.** `Hub Owner` holds the bytes `Team - Full Access` held when 134 ran. This
schema has no role hierarchy to inherit through — `roles` is a flat row with a jsonb blob — so a permission
granted to `Team - Full Access` *later* does NOT appear on `Hub Owner`. **Anyone adding a page key to
`Team - Full Access` from now on must decide, explicitly, whether `Hub Owner` gets it too.** That is the
cost of not widening a live role, and it is written into 134's header so the next lane does not assume
inheritance.

**3. An existing `Hub Owner` row is never rewritten.** If the name is already taken — a re-run, or a store
that built or edited its own — the whole create is skipped and a NOTICE says so. `ON CONFLICT DO UPDATE`
would have made every re-run an escalation, which is precisely the foot-gun migration 131's header
documents (a hand-run topping every role back up to a set it had deliberately been trimmed out of).

**4. The map guard stays as tight as 133's.** The UPDATE matches only the row still holding
`'Team - Full Access'`, so a store that re-pointed `owner` itself keeps its choice, and on a database where
133 could not run (no `Team - Full Access` row at all) 134 does not run ahead of it — the row is left on
126's seed.

## Idempotence and the failure path — run, not reasoned

`server/tests/hub-sso/w8-jit-role.mjs` grew from 20 to **37** assertions. RED (no `134_*.sql` on disk)
**17 passed / 13 failed** → GREEN **37 passed / 0 failed**. New blocks:

- **J1b/J1c/J1d** — the owner's role carries `kpi-system:access`; `Team - Full Access` still does NOT
  (negative control, green on BOTH sides of the fix); no role but `Hub Owner` carries the key at all; and
  `Hub Owner`'s key set is exactly `Team - Full Access`'s plus that one key, same actions on every key.
- **J9** — the whole map equals 133's map with one row moved.
- **K1-K4** — re-applying 134 changes nothing (roles, permissions, map rows incl. `updated_at`, and
  `user_roles`, all snapshot-compared); a store that re-pointed `owner` keeps its choice; a store that
  edited `Hub Owner` keeps its edit; and a hub owner still lands on `Hub Owner` after three re-runs.
- **L1-L6 — the failure path, executed:** the schema is rebuilt from the real migration files with
  `031_seed_page_permissions.sql` SKIPPED, 134 applied, and the pg client's `notice` events read. `Hub
  Owner` is created from 031's page keys + `kpi-system:access` (no `orders:access` — that is 086's grant to
  a role that does not exist there), the NOTICE names the missing role, and the map row is not re-pointed.

**A test bug the RED run caught:** J9's first draft compared `JSON.stringify` of two objects. `SELECT` has
no key order and `JSON.stringify` does, so it failed red for the wrong reason. It compares a sorted `k=v`
string now. A test that can fail for a reason other than the one it names is not a test.

## Migration numbering

`134_hub_owner_kpi_role.sql`, appended at the END of `server/migrations/order.json` (**118** entries, and
118 `.sql` files on disk). `order.json`'s `_doc` said "the next free HUB number is 131", which was stale by
three lanes; it now reads 128/129/130 → S4-SB, 131 → S4-SB2, 132 → W6, 133 → W8b, 134 → W8c, **next free
135**. Empty-database run: `Successfully ran 118 migration(s)` / `applied: 118 | pending: 0 | mismatches: 0`,
and the identical line on the second run and on `--dry-run`.

## Still open, unchanged by this lane

Everything in W8b's "REPORTED, NOT FIXED" table, the 375 px shell overflow, and the A6 scope guard's SKIP
off the store-code lane (`A6_ENFORCE=1` on the integration branch) are all exactly as W8b left them.
