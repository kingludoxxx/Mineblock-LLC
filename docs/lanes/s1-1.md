# Lane handoff — S1-1 (sandbox pair born from nothing, dashboard half)   last session 2026-09-10   commit d008650

## Where I stopped (exact step, file:line)

Brief complete on this half. Branch `day2/s1-sandbox`, **rebased onto `hub/main`
= 54e77b6** mid-lane (Lane C3 landed migration 127 while this lane ran), so the
empty-database proof is 111 migrations, not the brief's 110. Nothing pushed,
nothing merged, nothing deployed. No live service touched.

1. `server/seeds/seed_superadmin.js:14-35` — `requiredCredential()` +
   `assertSuperAdminEnv()`. `SUPERADMIN_EMAIL` / `SUPERADMIN_PASSWORD` are now
   REQUIRED with no fallback; the two hard-coded Mineblock literals are gone.
   `server/seeds/run.js:8` calls the preflight **before the first write**, so a
   missing credential leaves no half-seeded database.
2. `server/src/config/dbSsl.js` (new) — `dbSslEnabled()`, the one place the
   database TLS decision is made. `server/src/config/db.js:9`,
   `server/src/db/pg.js:21` and `server/src/services/analyticsDb.js:46` now call
   it instead of each inferring `DATABASE_URL.includes('render.com') ||
   NODE_ENV === 'production'`. `DATABASE_SSL` unset = the historical inference,
   byte-for-byte.
3. `server/config/env.SB.example` (new) — the smallest env that boots this
   codebase on an empty database and serves login plus a newborn store's empty
   pages, with every optional integration unset and the panel each one makes
   dormant named.
4. `server/tests/sandbox/seed-superadmin-env.mjs`, `server/tests/sandbox/db-ssl.mjs`
   (new). Run them one file at a time — `node --test server/tests/sandbox/`
   (a directory) is not supported on this Node.

## What is proven (proof pack link) and what is not

`~/tasks/multistore-hub/briefs/out/PROOF-S1-1.md` — verbatim red and green for
both bugs, the full migrate/seed/boot/5-route output, and the isolation reads.

**Proven:** an empty `sb_empty` → 111 applied / 0 pending / 0 mismatches /
`other stores: 0`; `_store_identity` = `SB`; seeds create exactly one user, this
store's; boot under `NODE_ENV=production` with the example's values; health,
login, brand, store-config, orders and creative routes all 200; zero 5xx in the
whole boot log; `SHOPIFY_STORE_DOMAIN` unset needs **no placeholder**.

**Not proven:** anything on Render — no Render, Atlas, Shopify or Whop call was
made. `GET /` and `/login` are 404 in this worktree because `client/dist` was
never built locally; the login **API** is 200. The runbook
(`briefs/out/SB-PAIR-RUNBOOK.md`) carries the expected body of every
verification call for the lead to cross-check against.

## Open questions for the lead (not for Ludo)

1. **Boot seed vs one-off job** for the Render super-admin. Both work and both
   are written up (runbook §1). Boot seed is the default; the one-off keeps the
   password out of the service env. Lead picks.
2. `STRICT_MIGRATIONS=1` — deliberately left off; turn it on only after the
   first successful migrate on `sb-db`.

## Next action for the next session (one line)

Nothing on this half — the lead creates `sb-dashboard` from
`~/tasks/multistore-hub/briefs/out/SB-PAIR-RUNBOOK.md` §1-3.
